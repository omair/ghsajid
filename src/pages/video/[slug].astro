---
import { getCollection } from "astro:content";
import Piece from "../../layouts/Piece.astro";

export async function getStaticPaths() {
  const entries = await getCollection("videos");
  return entries.map((entry) => ({
    params: { slug: entry.data.slug },
    props: { entry },
  }));
}

const { entry } = Astro.props;
const { source, video_id, url, description, recorded } = entry.data;
---

<Piece title={entry.data.title} label="آواز">
  {
    source === "youtube" ? (
      <iframe
        class="embed"
        loading="lazy"
        src={`https://www.youtube-nocookie.com/embed/${video_id}`}
        title={entry.data.title}
        allowfullscreen
      />
    ) : (
      <p>
        <a href={url}>فیس بک پر سنیے</a>
      </p>
    )
  }
  {description && <p class="work__note">{description}</p>}
  {
    recorded && (
      <p class="colophon">ریکارڈنگ ۔ {recorded.toISOString().slice(0, 10)}</p>
    )
  }
</Piece>
