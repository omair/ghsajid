---
import { getCollection } from "astro:content";
import Base from "../../layouts/Base.astro";

const entries = (await getCollection("ghazals")).sort((a, b) =>
  a.data.title.localeCompare(b.data.title, "ur"),
);
---

<Base title="غزلیں">
  <h1 class="piece__title">غزلیں</h1>
  <p class="piece__label">مطلع کے مطابق</p>
  <ul class="listing">
    {
      entries.map((entry) => (
        <li>
          <a href={`/ghazal/${entry.data.slug}`}>
            {entry.data.title}
            
          </a>
        </li>
      ))
    }
  </ul>
</Base>
